import React, { useState } from 'react';import { Menu, ChevronRight, Smartphone, ShieldCheck, Wrench, Lock, LayoutDashboard, Settings, Users, Database } from 'lucide-react';const NundaApp = () => {  const [isLoggedIn, setIsLoggedIn] = useState(false);  const [showLogin, setShowLogin] = useState(false);  const [phone, setPhone] = useState('');  const [password, setPassword] = useState('');  // Lógica da Porta do Criador  const handleLogin = (e) => {    e.preventDefault();    if (phone === '972746195' && password === '654321') {      setIsLoggedIn(true);      setShowLogin(false);      alert("Acesso de Criador Concedido. Permissões ilimitadas ativadas.");    } else {      alert("Credenciais incorretas.");    }  };  return (    <div className="min-h-screen bg-white font-sans text-slate-900 relative">            {/* --- BOTÃO OCULTO PARA O CRIADOR (No ícone do Menu) --- */}      <nav className="flex items-center justify-between px-6 py-4 border-b border-gray-100 sticky top-0 bg-white z-50">        <div className="flex items-center gap-2">           <div className="bg-blue-900 p-2 rounded-lg">             <div className="grid grid-cols-2 gap-0.5 w-5 h-5"><div className="bg-white rounded-sm"></div><div className="bg-white rounded-sm"></div><div className="bg-white rounded-sm"></div><div className="bg-white rounded-sm"></div></div>           </div>           <div>             <h1 className="text-2xl font-black text-blue-900 leading-none">NUNDA</h1>             <p className="text-[8px] tracking-[0.2em] font-bold text-gray-400">CONSTRUIR • INOVAR • CRESCER</p>           </div>        </div>        {/* Clique longo ou duplo aqui poderia abrir o login */}        <button onClick={() => setShowLogin(true)}>           <Menu className="text-blue-900 w-8 h-8" />        </button>      </nav>      {/* --- INTERFACE DO CRIADOR (Só aparece se logado) --- */}      {isLoggedIn && (        <div className="bg-red-50 border-b-2 border-red-200 p-4 animate-pulse">          <div className="flex items-center justify-between">            <div className="flex items-center gap-2 text-red-700 font-bold text-sm">              <Lock className="w-4 h-4" /> MODO CRIADOR ATIVADO (Acesso Total)            </div>            <button onClick={() => setIsLoggedIn(false)} className="text-xs bg-red-200 px-2 py-1 rounded">Sair</button>          </div>          <div className="grid grid-cols-4 gap-2 mt-4">             <button className="flex flex-col items-center bg-white p-2 rounded-xl shadow-sm"><Database size={20}/> <span className="text-[10px]">Dados</span></button>             <button className="flex flex-col items-center bg-white p-2 rounded-xl shadow-sm"><Users size={20}/> <span className="text-[10px]">Usuários</span></button>             <button className="flex flex-col items-center bg-white p-2 rounded-xl shadow-sm"><LayoutDashboard size={20}/> <span className="text-[10px]">Painel</span></button>             <button className="flex flex-col items-center bg-white p-2 rounded-xl shadow-sm"><Settings size={20}/> <span className="text-[10px]">Config</span></button>          </div>        </div>      )}      {/* --- CONTEÚDO PRINCIPAL --- */}      <main className="px-6 pt-10 pb-20">         {/* ... (Mesmo conteúdo anterior da Hero Section) ... */}         <div className="inline-flex items-center gap-2 bg-blue-50 border border-blue-100 rounded-full px-4 py-1.5 mb-8">          <Wrench className="w-4 h-4 text-blue-900" />          <span className="text-[10px] font-bold uppercase tracking-widest text-blue-900">Engenharia de Precisão</span>        </div>        <h2 className="text-5xl font-black leading-[0.9] text-slate-900 mb-8">CONSTRUIR.<br /><span className="text-blue-800">INOVAR.</span><br />CRESCER.</h2>                {/* Botão de Iniciar que, se for criador, pode ter funções extras */}        <button className={`w-full font-bold py-5 px-8 rounded-2xl flex items-center justify-center gap-3 transition ${isLoggedIn ? 'bg-red-600 shadow-red-200' : 'bg-blue-900 shadow-blue-200'} text-white shadow-xl`}>          {isLoggedIn ? 'GERENCIAR SISTEMA' : 'INICIAR PROJETO'} <ChevronRight className="w-5 h-5" />        </button>      </main>      {/* --- MODAL DE LOGIN (A PORTA) --- */}      {showLogin && !isLoggedIn && (        <div className="fixed inset-0 bg-black/60 backdrop-blur-sm z-[100] flex items-center justify-center p-6">          <div className="bg-white w-full max-w-sm rounded-[32px] p-8 shadow-2xl">            <h3 className="text-2xl font-black text-blue-900 mb-6 flex items-center gap-2">              <Lock /> Acesso Restrito            </h3>            <form onSubmit={handleLogin} className="space-y-4">              <div>                <label className="text-xs font-bold text-gray-400 uppercase">Número do Criador</label>                <input                   type="text"                   value={phone}                  onChange={(e) => setPhone(e.target.value)}                  className="w-full bg-gray-100 border-none rounded-xl p-4 mt-1 focus:ring-2 focus:ring-blue-500"                  placeholder="972746195"                />              </div>              <div>                <label className="text-xs font-bold text-gray-400 uppercase">Senha Mestra</label>                <input                   type="password"                   value={password}                  onChange={(e) => setPassword(e.target.value)}                  className="w-full bg-gray-100 border-none rounded-xl p-4 mt-1 focus:ring-2 focus:ring-blue-500"                  placeholder="******"                />              </div>              <div className="flex gap-2 pt-2">                <button type="button" onClick={() => setShowLogin(false)} className="flex-1 py-4 font-bold text-gray-500">Cancelar</button>                <button type="submit" className="flex-1 bg-blue-900 text-white py-4 rounded-2xl font-bold shadow-lg shadow-blue-200">Entrar</button>              </div>            </form>          </div>        </div>      )}    </div>  );
